diff -urN fdict-0.7.1.old/Makefile.project fdict-0.7.1.new/Makefile.project
--- fdict-0.7.1.old/Makefile.project	2019-04-16 21:33:43.000000000 +0200
+++ fdict-0.7.1.new/Makefile.project	2019-09-05 17:05:21.253879341 +0200
@@ -38,22 +38,24 @@
   PIC = 1
 endif
 
+# Set the default install script
+INSTALL ?= install -p
 
 # Create custom installation of modules
 .NOTPARALLEL: install-mod
 smeka-install: install-mod
 install-mod: $(F_MODS)
-	install -pm $(_OCT_rwrr) -t $(FMOD_DIR) $(F_MODS)
+	$(INSTALL) -m $(_OCT_rwrr) -t $(DESTDIR)$(FMOD_DIR) $(F_MODS)
 
 .NOTPARALLEL: install-settings
 smeka-install: install-settings
 install-settings: settings.bash
-	install -pm $(_OCT_rwrr) -t $(BIN_DIR) settings.bash
+	$(INSTALL) -m $(_OCT_rwrr) -t $(DESTDIR)$(BIN_DIR) settings.bash
 
 .NOTPARALLEL: install-header
 smeka-install: install-header
 install-header: fdict.inc
-	install -pm $(_OCT_rwrr) -t $(INC_DIR) fdict.inc
+	$(INSTALL) -m $(_OCT_rwrr) -t $(DESTDIR)$(INC_DIR) fdict.inc
 
 
 # Force the deletion of both libraries
diff -urN fdict-0.7.1.old/smeka/Makefile.directory fdict-0.7.1.new/smeka/Makefile.directory
--- fdict-0.7.1.old/smeka/Makefile.directory	2019-04-16 21:33:43.000000000 +0200
+++ fdict-0.7.1.new/smeka/Makefile.directory	2019-09-05 17:11:19.187430499 +0200
@@ -16,7 +16,8 @@
 
 
 # Default directories
-PREFIX ?= $(HOME)/$(PROJECT_NAME)
+prefix ?= $(HOME)/$(PROJECT_NAME)
+PREFIX ?= $(prefix)
 # Include directory (.h)
 INC_DIR ?= $(PREFIX)/include
 # Module directory (.mod)
diff -urN fdict-0.7.1.old/smeka/Makefile.install fdict-0.7.1.new/smeka/Makefile.install
--- fdict-0.7.1.old/smeka/Makefile.install	2019-04-16 21:33:43.000000000 +0200
+++ fdict-0.7.1.new/smeka/Makefile.install	2019-09-05 17:11:34.803451217 +0200
@@ -1,4 +1,5 @@
 _SMEKA_install = 1
+INSTALL ?= install -p
 
 # Basic implementation of installing the different
 # libraries etc.
@@ -13,16 +14,16 @@
 .NOTPARALLEL: smeka-install-lib
 smeka-install-lib: smeka-install-init-dir
 ifneq ($(LIBRARIES),)
-	install -pm $(_OCT_rwxrxrx) -t $(LIB_DIR) $(LIBRARIES)
+	$(INSTALL) -m $(_OCT_rwxrxrx) -t $(DESTDIR)$(LIB_DIR) $(LIBRARIES)
 endif
 
 
 .PHONY: smeka-install-init-dir
 .NOTPARALLEL: smeka-install-init-dir
 smeka-install-init-dir:
-	-mkdir -p $(INC_DIR)
-	-mkdir -p $(LIB_DIR)
-	-mkdir -p $(BIN_DIR)
+	-mkdir -p $(DESTDIR)$(INC_DIR)
+	-mkdir -p $(DESTDIR)$(LIB_DIR)
+	-mkdir -p $(DESTDIR)$(BIN_DIR)
 
 
 # Issuing uninstall will forcefully remove the installation directory
@@ -31,14 +32,14 @@
 smeka-uninstall-install:
 	@echo ""
 	@echo "The directory tree:"
-	@echo "  $(PREFIX)"
+	@echo "  $(DESTDIR)$(PREFIX)"
 	@echo "will be deleted!"
 	@echo "You may abort the operation within 2 secs by pressing:"
 	@echo ""
 	@echo "  Ctrl+C or ^C"
 	@echo ""
 	@sleep 2
-	@rm -rf $(PREFIX)
+	@rm -rf $(DESTDIR)$(PREFIX)
 uninstall: smeka-uninstall-install
 
 
Binary files fdict-0.7.1.old/src/.Makefile.inc.swp and fdict-0.7.1.new/src/.Makefile.inc.swp differ
diff -urN fdict-0.7.1.old/src/Makefile.inc fdict-0.7.1.new/src/Makefile.inc
--- fdict-0.7.1.old/src/Makefile.inc	2019-04-16 21:33:43.000000000 +0200
+++ fdict-0.7.1.new/src/Makefile.inc	2019-09-05 16:53:31.306336237 +0200
@@ -37,7 +37,7 @@
 
 
 # Ensure that the objects are built with the correct module path
-$(src_FPP_SOURCES): INCLUDES += -I$(src_DIR)
+$(src_FPP_SOURCES): INCLUDES += -I. -I$(src_DIR) 
 
 .PHONY: source-src
 source-src: $(src_SOURCES) fdict.inc
